services:
  - type: web
    name: high-danger-forecast
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --preload --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
